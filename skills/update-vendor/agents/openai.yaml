interface:
  display_name: "Update Vendor"
  short_description: "Pull, push, and propagate configured Git vendors"
  default_prompt: "Use $update-vendor to pull configured vendors or propagate this tree to sibling consumers."
